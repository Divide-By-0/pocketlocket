// Copy this file to stripe-config.js and fill in your actual values
// NEVER commit stripe-config.js to git (it's in .gitignore)

window.STRIPE_CONFIG = {
  publishableKey: 'pk_test_your_actual_key_here', // Get from Stripe Dashboard
  backendUrl: 'https://your-actual-backend.com'   // Your backend URL
};
