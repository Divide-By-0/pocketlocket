# Copy this file to .env and fill in your actual values
# NEVER commit .env to git (it's in .gitignore)

# Stripe Secret Key (get from https://dashboard.stripe.com/apikeys)
# Use test key (sk_test_) for development, live key (sk_live_) for production
STRIPE_SECRET_KEY=sk_test_your_secret_key_here

# Your domain (used for success/cancel URLs)
DOMAIN=http://localhost:3000

# Server port
PORT=3000

# Optional: Stripe Webhook Secret (if using webhooks)
# STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here
